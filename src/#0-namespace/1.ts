// 命名空间，用于组织代码，现在基本都用module了
// 本项目中，namespce用来隔离示例代码，例如：
namespace N1 {
  export class User {
    name: string;

    constructor(name: string) {
      this.name = name;
    }
  }
}

namespace N2 {
  export class User {
    name: string;

    constructor(name: string) {
      this.name = name;
    }
  }
}
